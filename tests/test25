let a: u32;
let b: u32;
let c: u32;

fn main(): u8
{
  let z: char;
  let y: u32;
  let x: u32;
  x= 10; y= 20; z= 30;
  printint(x); printint(y); printint(z);
  a= 5; b= 15; c= 25;
  printint(a); printint(b); printint(c);
  return 0;
}