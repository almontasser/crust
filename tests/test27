fn param8(a: u32, b: u32, c: u32, d: u32, e: u32, f: u32, g: u32, h: u32): u32{
    printint(a); printint(b); printint(c); printint(d);
    printint(e); printint(f); printint(g); printint(h);
    return 0;
}

fn param5(a: u32, b: u32, c: u32, d: u32, e: u32): u32{
    printint(a); printint(b); printint(c); printint(d); printint(e);
    return 0;
}

fn param2(a: u32, b: u32): u32{
    let c: u32; let d: u32; let e: u32;
    c = 3; d = 4; e = 5;
    printint(a); printint(b); printint(c); printint(d); printint(e);
    return 0;
}

fn param0(): u32{
    let a: u32; let b: u32; let c: u32; let d: u32; let e: u32;
    a = 1; b = 2; c = 3; d = 4; e = 5;
    printint(a); printint(b); printint(c); printint(d); printint(e);
    return 0;
}

fn main(): u32{
    param8(1, 2, 3, 4, 5, 6, 7, 8);
    param5(1, 2, 3, 4, 5);
    param2(1, 2);
    param0();
    return 0;
}