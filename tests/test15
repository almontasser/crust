fn main(): u8 {
  let  a: u8;  let b: *u8;  let c: u8;
  let  d: u8; let e: *u8; let f: u8;

  a= 18; printint(a);
  b= &a; c= *b; printint(c);

  d= 12; printint(d);
  e= &d; f= *e; printint(f);
  return 0;
}