fn main(): int {
  let  a: u8;  let b: *u8;  let c: u8;
  let  d: int; let e: *int; let f: int;

  a= 18; printint(a);
  b= &a; c= *b; printint(c);

  d= 12; printint(d);
  e= &d; f= *e; printint(f);
  return 0;
}